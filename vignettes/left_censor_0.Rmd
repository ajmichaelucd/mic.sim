---
title: "left_censor_0"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{left_censor_0}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(mic.sim)
```

```{r}

d1 = tibble(L = rexp(n = 1000), R = L) 
d1$L[1] = 0
library(survival)

survreg(Surv(L,R, type = "interval2") ~ 1, data = d1, dist = "gaussian")
survfit(Surv(L,R, type = "interval2") ~ 1, data = d1)
```

```{r}

d2 = tibble(Y = rnorm(n =1000), status = 1)
survreg(Surv(Y,status)~1, data = d2)
```

